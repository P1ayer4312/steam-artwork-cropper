# Image Cropper for Steam Artwork Showcase

A simple script that measures and crops images for Steam artwork.

[p1ayer4312.github.io/steam-crop](https://p1ayer4312.github.io/steam-crop)

WIP rewriting the whole thing

Small patch pushed to fix a transparency issue, no plans to add new features here will try to find time to continue working on the `refactor` branch.

Running this:

```shell
npm install
npm run webpack
```

Run `index.html` from within `dist` as a local server, you can use the Live Server extension from VSCode
